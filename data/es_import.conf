input {
      file {
          path => "/Users/cdickson/Documents/workspace/TorFlow/data/Relays_1.csv"
          type => "relay"
          start_position => "beginning"
      }
}

filter {
    csv {
        columns => ["name","fingerprint","ip","port","bandwidth","gps"]
        separator => ","
    }
}

output {
    elasticsearch {
        action => "index"
        host => "localhost"
        index => "relays_1"
        workers => 1
    }
}