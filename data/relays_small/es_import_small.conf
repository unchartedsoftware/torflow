input {
      file {
          path => "/Users/cdickson/workspace/TorFlow/data/relays_small/data/*.csv"
          type => "relay"
          start_position => "beginning"
      }
}

filter {
    csv {
        columns => ["name","fingerprint","flags","ip","port","bandwidth","uptime","gps","date"]
        separator => ","
    }
}

output {
    elasticsearch {
        action => "index"
        host => "localhost"
        index => "relays_small"
        workers => 1
    }
}